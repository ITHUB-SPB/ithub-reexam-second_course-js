/**
 * @description 
 * Принимает на вход объект и название ключей и возвращает 
 * объект с информацией о наличии каждого из них.
 * 
 * @privateRemarks
 * до 1.75 баллов
 * 
 * @param {Object} object - Проверяемый объект
 * @param {String[]} keys - Проверяемые ключи
 * @returns {Object<string, boolean>}
 * @throws Выбрасывает исключение на пустом массиве ключей
 * 
 * @example
 * // returns { particle: true, tag: false }
 * сheckObject({ id: 1, particle: 10 }, ["particle", "tag"])
 *  
 */
export function checkObject(object, keys) {
    return 0
}
