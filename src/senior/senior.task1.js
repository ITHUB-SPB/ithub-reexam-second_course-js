/**
 * @description 
 * Принимает на вход полное имя и возвращает объект по образцу
 * 
 * @privateRemarks
 * до 2 баллов
 * 
 * @param {String} fullName - Имя
 * 
 * @returns {{firstName: String, lastName?: String}}
 *
 * @example
 * // returns { firstName: "John", lastName: "Al" }
 * splitName("John Al")
 * 
 * @example
 * // returns { firstName: "Oliver" }
 * splitName("Oliver")
 * 
 */
export function splitName(fullName) {
    return
}

/**
 * @description 
 * Принимает на вход массив имен и возвращает новый
 * массив, полученный их трансформацией функцией `splitName`
 * 
 * @param {String[]} namesArray - Массив имён
 * 
 * @returns {{firstName: String, lastName?: String}[]}
 *
 * @example
 * // returns [{ firstName: "John", lastName: "Al" }, { firstName: "Oliver" }]
 * toSplittedNames(["John Al", "Oliver"])
 * 
 */
export function toSplittedNames(namesArray) {
    return
}
