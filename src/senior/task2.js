/**
 * @param {Array<Object | Array<number>>} initialArray - Исходный массив
 * @returns {Number[][]} Массив численных массивов
 *
 * @description Функция получает массив и должна модифицировать
 * его таким образом, чтобы все объекты трансформировались в
 * массивы со значениями свойств. Необходимо изменить текущий
 * массив и вернуть его. Для проверки на объект можно использовать
 * отрицание проверки `Array.isArray`.
 *
 * @example
 * const data = [ [ 1 ], { id: 40 }, [ 100 ], { part: 10 } ]
 * console.log(generateArray(data)) // [ [1], [40], [100], [10] ]
 * console.log(data) // [ [1], [40], [100], [10] ]
 */
export function generateArray(initialArray) {
    return 0
}
