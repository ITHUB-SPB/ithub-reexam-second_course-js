/**
 * @description 
 * Функция получает массив и должна модифицировать его таким образом, 
 * чтобы все объекты трансформировались в массивы со значениями свойств. 
 * Необходимо изменить текущий массив и вернуть его. Для проверки на 
 * объект можно использовать отрицание проверки `Array.isArray`.
 *
 * @privateRemarks
 * до 1.5 баллов
 * 
 * @param {Array<Object | Array<number>>} initialArray - Исходный массив
 * @returns {Number[][]} Массив численных массивов
 *
 * @example
 * // returns [ [1], [40], [100], [10] ]
 * generateArray([ [ 1 ], { id: 40 }, [ 100 ], { part: 10 } ])
 * 
 */
export function generateArray(initialArray) {
    return 0
}
